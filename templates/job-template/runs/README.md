# Runs

Each workflow execution creates `runs/<run-id>/` with prompt packets, stage outputs, structured research and judge JSON artifacts, a run-level `sources.json`, stage claim sidecars, logs, and audit files.
